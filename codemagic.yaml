workflows:
  main:
    name: Build APK
    environment:
      flutter: 2.10.3
    scripts:
      - name: Install dependencies
        script: ./gradlew dependencies
      - name: Build APK
        script: ./gradlew assembleRelease